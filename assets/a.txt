-- Paso 1: Modificar la columna 'tipo' en la tabla 'inventario'
-- para añadir 'produccion' al ENUM.
-- Asegúrate de que esta sentencia se ejecuta con precaución,
-- ya que ALTER TABLE puede bloquear la tabla temporalmente.
ALTER TABLE `inventario` MODIFY COLUMN `tipo` ENUM('alimento','medicamento','herramienta','semilla','fertilizante','equipo','produccion','otro') NOT NULL;

-- Paso 2: Insertar la nueva categoría 'Producción' en la tabla 'categorias_inventario'.
-- Si ya tienes un ID específico en mente o si el auto_increment no es el siguiente,
-- ajusta el valor del ID según sea necesario.
INSERT INTO `categorias_inventario` (`nombre`, `descripcion`) VALUES ('Producción', 'Productos derivados de animales (leche, huevos, carne, etc.)');
